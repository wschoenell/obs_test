setupRequired(afw)
setupRequired(daf_base)
setupRequired(daf_butlerUtils)
setupRequired(pex_policy)
setupRequired(pex_config)
# this is used by bin/genInputRegistry.py, which is only needed when creating a data repo
setupOptional(skypix)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
